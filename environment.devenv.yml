name: abc-test

dependencies:
  - python ={{ get_env("PY_VER", default="3.9") }}
  - cmake>=3.18
  - ninja
  - c-compiler
  - cxx-compiler
  - make  # [unix]
  - readline=8.1  # [unix]
  - zlib
  - libpng
  - dirent  # [win]
